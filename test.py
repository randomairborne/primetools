import primetools
print(f"Primes to 100: `{primetools.get_primes_to(100)}`")
print(f"`{primetools.is_prime(999999937)}` If this isn't `True` something is broken")
print(f"`{primetools.is_prime(999999935)}` If this isn't `False` something is broken")
